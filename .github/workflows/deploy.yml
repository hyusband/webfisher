name: Deploy a Vercel Automático

on:
  push:
    branches:
      - main # Cambia esto por tu rama principal si es otra

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}